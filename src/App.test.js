describe('Jest', () => {
    it('should work', () => {
        expect(1).toBe(1);
    })
    it('is working', () => {
        expect(2).toBe(2);
    })
    it('is ready', () => {
        expect(3).toBe(3);
    })
})